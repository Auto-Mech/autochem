pre-commit:
  parallel: true
  commands:
    ruff-linter:
      run: pixi run -e lint ruff check --select I --fix
    run-format:
      run: pixi run -e lint ruff format --check

pre-push:
  commands:
    # old:
    pylint-linter:
      run: pixi run -e lint pylint --rcfile=.pylintrc automol
